import express from "express";
// import { createLogs, getLogs } from "../controllers/productLogs.controller";

let router = express.Router();

// router.post("/createLogs", createLogs);

// router.get("/getLogs", getLogs);

export default router;
